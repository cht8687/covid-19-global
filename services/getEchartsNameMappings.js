import world from '../const/echartsNameMappings/world';

export default function(name) {
  switch (name) {
    case 'world':
      return world;
      break;
    case y:
      // code block
      break;
    default:
    // code block
  }
}
